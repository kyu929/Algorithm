#include<iostream>
using namespace std;

int main() {
	int N;
	int a;
	int max = -1000000, min = 10000000;
	scanf_s("%d", &N);
	while (N--) {
		scanf_s("%d", &a);
		if (a > max) {
			max = a;
		}
		if (a < min) {
			min = a;
		}
	}
	printf("%d %d", min, max);
	return 0;
}

